// const fullDisclosure = (disclosure, { schemeManagers, issuers, credentialTypes }) => {
//
// }
//
// export default (disclosures, irmaConfiguration) =>
//   disclosures.map(disclosure => fullDisclosure(disclosure, irmaConfiguration));
